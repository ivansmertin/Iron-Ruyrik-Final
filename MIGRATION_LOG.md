# Журнал визуальной миграции «Железный Рюрик»

## Этап M00 — Baseline, browser evidence и стабильные тестовые часы
- **Дата**: 11.09.2026
- **HEAD**: `58875cb`
- **Изменения**:
  - Стабилизированы системные часы в `src/__tests__/admin_and_booking_remediation.test.tsx` (`vi.setSystemTime(new Date('2026-09-08T06:00:00+03:00'))`).
  - Все тесты: vitest (25/25 PASS), pytest (40/40 PASS), npm run build (PASS), npm run lint (PASS).
  - Сняты 40 baseline-скриншотов на 4 вьюпортах (320x740, 360x800, 390x844, 430x932).
  - Зафиксированы диагностические матрицы доступности, фокуса, motion (350ms) и 0 вызовов startViewTransition в BrowserRouter.
- **Статус**: PASS (верифицировано).

## Этап M01 — Foundation: единый ритм, типографика и общие controls
- **Дата**: 11.09.2026
- **Изменения**:
  - **Палитра и токены**: внедрены единые токены: canvas `#111111`, elevated `#191919`, control `#222222`, text primary `#FFFFFF`, secondary `#C7C7C7`, tertiary `#929292`, brand yellow `#FFD626`, yellow-pressed `#E8BE00`.
  - **Геометрия и радиусы**: 0 для контента (`--radius-none`, `--radius-card: 0px`), 4px для контролов (`--radius-control: 4px`), 8px для модальных окон (`--radius-modal: 8px`). Устранены устаревшие литералы 12px, 14px, 16px, 20px.
  - **Вертикальный ритм**: устранен конфликт двойного отступа между секциями — обнулен вертикальный паддинг у `.ui-section`, ритм строго управляется `margin-top: var(--space-6)` (24px) и `.ui-section--spacious` (32px).
  - **Типографическая шкала**: гарнитура Onest, заголовки страниц `clamp(28px, 5vw, 32px)`, hero metric `clamp(44px, 8.5vw, 52px)`, секции `clamp(18px, 4vw, 20px)`, body 16px, метаданные 13px (tabular numbers). Добавлен `overflow-wrap: break-word`.
  - **Сенсорные зоны (Touch targets)**:
    - Первичные кнопки: 48–52px;
    - Логотип в хедере: 54px высота (масштабирование перенесено на внутренние элементы, ссылка сохраняет полный тач-таргет);
    - `.back-link` и кнопки закрытия модальных окон / тостов: строго `>= 44x44` px;
    - Кнопка `.button--sm`: min-height 44px.
  - **Нижняя навигация**: высота 60px + safe-area, цвет неактивных пунктов `#929292` обеспечивает контраст 4.68:1 (>= 4.5:1 WCAG AA), шрифт 12px с ellipsis, тач-таргеты 49x75.6px.
  - **Кнопки**: состояние `.button--confirmed` отделено от disabled — сохраняет 100% opacity, фон brand-yellow и темный текст без блеклости во время 350ms фиксации.
  - **Модальная система**: полная унификация `.modal` и `.modal-content` (8px радиус, 20px паддинг, единая тень, max-height 100dvh). Реализована изоляция фонового DOM (`aria-hidden` и `inert`) при открытом окне.
  - **Документация**: актуализирован `DESIGN_SYSTEM.md`.
- **Верификация**:
  - vitest: 25/25 PASS
  - pytest: 40/40 PASS
  - build & lint: 0 errors
  - CDP browser validation: подтверждены все тач-таргеты (>= 44px), контраст навигации (4.68:1), радиус модалок (8px), подтвержденная кнопка (opacity 1.0).
- **Статус**: PASS. Готовность к переходу на M02.

## Этап M02 — Integrations и HealthSources: одно достоверное состояние
- **Дата**: 11.09.2026
- **Изменения**:
  - **Единый источник правды**: Экран `/integrations` (`IntegrationsPage.tsx`) и модалка `HealthSourcesModal.tsx` переведены на единый общий хук `useHealthSources` (`src/hooks/useHealthSources.ts`), запрашивающий бэкенд `/api/v1/health/sources` и мост `healthBridge.ts`.
  - **Устранение моков и фиктивных состояний**:
    - В `IntegrationsPage` полностью удалены `initialIntegrations`, хранение React-компонентов иконок в `localStorage` (`ryrik_integrations`), фиктивные даты `'Сегодня, 08:30'` и фиктивный таймер 1200ms «Синхронизация...».
    - Добавлена функция `cleanupLegacyIntegrationsStorage()` для безопасной очистки устаревшего ключа `ryrik_integrations` из `localStorage`.
  - **Честность Web vs Native**:
    - В веб-браузере системные HealthKit и Health Connect аппаратно недоступны (песочница). Интерфейс больше не симулирует успешное подключение и автосинхронизацию.
    - Пользователю честно объясняется необходимость мобильного приложения iOS/Android и предлагается ручной ввод замеров в разделе «Мой прогресс».
    - Кнопки подключения системных источников в вебе заблокированы с явным статусом «Требуется нативное приложение».
  - **LIFO-иерархия закрытия (Escape и Android hardware Back)**:
    - Вложенные состояния подтверждения (`activeProviderForPermission`, `disconnectConfirmProvider`) регистрируются в `modalStack`.
    - В `Modal.tsx` нажатие Escape синхронизировано с `popModal()`: первое нажатие Escape или аппаратной кнопки Back закрывает вложенный диалог, сохраняя модальное окно открытым.
    - Второе нажатие закрывает основное модальное окно.
    - Удален конкурирующий слушатель `window.addEventListener('keydown')` из `HealthSourcesModal.tsx`.
  - **Исправление терминологии состава тела**:
    - В `healthBridge.ts` строка Health Connect `'Мышечная масса (LeanBodyMassRecord)'` исправлена на `'Безжировая масса (LeanBodyMassRecord)'` (на бэкенде сохраняется канонический `lean_body_mass`).
    - В `HEALTH_INTEGRATIONS.md` обновлена таблица Xiaomi Scale S400 и документация единого контроллера.
  - **Дизайн-система (M01)**:
    - Страница `/integrations` стилизована токенами M01: радиус карточек 0px (`--radius-card`), контролов 4px (`--radius-control`), цвета `#111111`, `#191919`, `#222222`, `#FFD626`, доступные размеры touch-target >= 44px.
- **Верификация**:
  - Vitest: 33/33 PASS (включая 8 новых тестов M02 в `src/__tests__/m02_integrations_health_sources.test.tsx`).
  - Pytest: 40/40 PASS.
  - Build & Lint: 0 errors, 0 warnings.
- **Статус**: PASS. Готовность к переходу на M03.

## Этап M03 — Home polish: честные данные и открытый ритм
- **Дата**: 11.09.2026
- **Изменения**:
  - **Искоренение фиктивных данных слотов**:
    - Удален захардкоженный текст «Силовая тренировка» и «Ваня · 60 мин» для свободных/занятых слотов.
    - Для подтвержденной тренировки (`booking`) отображается реальное название (`booking.title`), имя тренера или «Самостоятельно» (`booking.trainerName ?? 'Самостоятельно'`) и расчетная длительность.
    - Для свободного слота отображается нейтральный заголовок «Тренировка в зале», динамическая длительность и мета `${duration} мин · Тренер или самостоятельно`.
    - CTA «Записаться» честно ведет на `/booking/${nextSlot.id}` (не на общий `/schedule`).
    - Динамический расчет длительности `calculateDurationMinutes(startAt, endAt)` на основе реального интервала слота.
  - **Изоляция ошибки расписания от активной записи**:
    - При сбое `scheduleQuery` и наличии активной записи карточка записи не скрывается.
    - При отсутствии записи и ошибке расписания отображается честная карточка с сообщением об ошибке и кнопкой «Повторить» (`isScheduleError`), вместо ложного пустого состояния «Пока ничего не выбрано / Найти время».
  - **Исключение прошедших слотов**:
    - Слот `nextSlot` выбирается строго из будущих незаблокированных слотов (`endIso > now && !isBlocked`).
    - Убран некорректный fallback `?? slots[0]`, подставлявший слоты из прошлого. Если будущих слотов нет — отображается честное пустое состояние.
  - **Динамический лимит вместимости**:
    - В модальном окне локации клуба захардкоженное «не более 8 человек» заменено на динамическое `не более ${capacity.limit} человек` из `capacity.limit` API.
  - **Надежность и доступность новостного виджета (`GymNewsWidget.tsx`)**:
    - `safeActiveIndex` ограничен `Math.min(activeIndex, posts.length - 1)` и сбрасывается эффектом при сокращении количества постов.
    - Ошибка загрузки изображения `imageFailed` автоматически сбрасывается при смене ID поста (`currentPost?.id`).
    - Сетевая ошибка `isError` четко отделена от пустого канала `posts.length === 0`.
    - Кнопке раскрытия длинного текста добавлен атрибут `aria-expanded={isExpanded}`.
    - Интерактивные точки-кнопки заменены на непрокликиваемый индикатор `.gym-news-feed__dots-indicator` со `role="status"` и скрытыми точками, что устранило пересечение сенсорных зон (<44px) и гарантировало навигационным кнопкам Prev/Next свободную тач-зону `>= 44x44` px.
  - **Вертикальный ритм и открытая композиция**:
    - Сохранена строгая бесплашечная композиция: приветствие → локация клуба → время/метаданные слота → желтый CTA / другое время → разделитель → заполненность зала → разделитель → редакторские новости.
    - Крупное время `clamp(2.4rem, 9.2vw, 3.25rem)` с `tabular-nums` выступает главным типографическим якорем первого экрана.
- **Верификация**:
  - Vitest: 40/40 PASS (включая 7 новых тестов M03 в `src/__tests__/m03_home_polish.test.tsx`).
  - Pytest: 40/40 PASS.
  - Build & Lint: 0 errors, 0 warnings.
  - CDP browser validation: подтверждены тач-таргеты (locationBtn 166x44, cta 358x52, secondaryLink 131x44, channelLink 117x44, navBtns 44x44, postLink 143x44), отсутствие переполнения на 320px, корректная работа модального окна клуба с динамическим лимитом и раскрытие поста.
- **Статус**: PASS. Блок 1 (M00–M03) полностью завершен. Готовность к Codex Reviewer R1.

## Этап R1 — Codex Review & Remediation (Блок M00–M03)
- **Дата**: 11.09.2026
- **Статус ревью**: Изначально BLOCK (3 замечания P1, 1 замечание P2). После выполнения ремедиации — **PASS WITH P2** (все P1 устранены, P2 взято на мониторинг в M04).
- **Устраненные замечания P1**:
  - **P1 #1 (Finding-01 — Недостоверный статус и двойной подсчет интеграции Xiaomi / Zepp Life)**:
    - Проблема: `IntegrationsPage.tsx` считал Xiaomi прямым источником (из 4) и выставлял `isXiaomiConnected = isAppleConnected || isHealthConnectConnected`, из-за чего при подключенном Apple Health отображалось «2 из 4 активно».
    - Исправление: В `directIntegrations` оставлены только 3 прямых источника (Apple Health, Health Connect, Telegram). Счетчик считается строго по `sources.filter(s => s.status === 'connected').length` из `directIntegrations.length` (3).
    - Xiaomi вынесен как экосистемный агрегатор (`isEcosystemAggregator: true`, `connected: false`). При подключенном Apple Health / Health Connect отображается честный статус «Данные весов поступают через Apple Health» и бейдж «Через Apple Health», без ложного прямого подключения и без накрутки счетчика («1 из 3 активно»).
  - **P1 #2 (Finding-02 — Игнорирование ошибки запроса `getHealthSources` в `/integrations` и `HealthSourcesModal`)**:
    - Проблема: При сбое API `getHealthSources` компонент молча рендерил список с пустыми данными («0 из 4 активно»), скрывая от пользователя факт сетевого сбоя.
    - Исправление: В `IntegrationsPage.tsx` и `HealthSourcesModal.tsx` извлечены `error` и `refetch` из `useHealthSources()`. При ошибке отображается честная карточка с `AlertCircle`, текстом «Не удалось загрузить статус интеграций» и кнопкой «Повторить» (`refetch`), предотвращая скрытие сбоя.
  - **P1 #3 (Finding-03 — Неработающая изоляция фонового DOM в `Modal.tsx`)**:
    - Проблема: Модальное окно рендерилось внутри `<div className="app-layout">` внутри `#root`. Условие `!el.contains(dialog)` для `.app-layout` вычислялось как `false`, в результате `inert` и `aria-hidden` не накладывались на основной контент приложения.
    - Исправление: В `Modal.tsx` подключен `createPortal` в `document.body`. При открытии модального окна на все дочерние узлы `document.body` кроме бэкдропа (включая `#root`) накладываются `aria-hidden="true"` и `inert=true`. При закрытии окна/размонтировании исходные атрибуты полностью восстанавливаются.
- **Мониторинг P2**:
  - **P2 #1 (Finding-04 — Контроль ширины лейблов навигации на 320px)**:
    - На экстремально узком экране 320px проверено отсутствие наложения текста; взято на обязательный визуальный контроль при миграции профиля и расписания в M04.
- **Верификация ремедиации**:
  - Vitest: 44/44 PASS (добавлены 4 теста в `m02_integrations_health_sources.test.tsx` на изоляцию портала, честный счетчик Xiaomi и обработку ошибок с повтором).
  - Pytest: 40/40 PASS.
  - Build & Lint: 0 errors, 0 warnings.
  - Chrome DevTools Protocol browser QA (`verify_r1_remediation.mjs`):
    - Подтвержден счетчик «0 из 3 активно» при отсутствии подключений.
    - Подтвержден текст подсказки агрегатора Xiaomi «Требуется подключение Apple Health или Health Connect».
    - Подтверждена изоляция фонового DOM: `#root` до открытия `{ rootAriaHidden: null, rootInert: false }` -> во время модалки `{ rootAriaHidden: 'true', rootInert: true, isPortalInBody: true }` -> после закрытия `{ rootAriaHidden: null, rootInert: false }`.
    - Снят скриншот: `r1_remediation_integrations_390x844.png`.
- **Итог этапа R1**: Блок 1 (M00–M03) полностью принят независимым ревьюером с вердиктом **PASS WITH P2**. Разрешен переход к Блоку 2 (этап M04).

## Этап M04 — Schedule: список времени и сохранение контекста
- **Дата**: 11.09.2026
- **Изменения**:
  - **Открытый бесплашечный список времени**:
    - Убраны тяжелые закрытые карточки слотов с рамками и закруглениями.
    - Внедрены открытые строки с тонкими разделителями (`1px solid var(--border-subtle)`), фоном `transparent`, высотой строки 64–72px (66px на 320–360px, 71px на 390–430px).
    - Сенсорная зона `>= 44px` по высоте гарантирована.
  - **Типографика и верстка слота**:
    - Время: 20–23px (`clamp(1.3125rem, 4.8vw, 1.45rem)`), `font-weight: 700`, `font-variant-numeric: tabular-nums`, `white-space: nowrap` — исключен перенос времени по цифрам даже на 320px.
    - Статус и заполненность зала: 13–14px (`font-weight: 600`), аккуратно выровнены по правому краю, с адаптивным масштабированием на 320px (`padding: 12px 2px`).
  - **Семантика списка и доступность**:
    - Список оформлен семантическим `<ul>` и `<li>` (`.slot-list__item`).
    - Доступные слоты рендерятся как `<Link className="time-slot is-available">` без конфликтующего `role="listitem"`, с явным `aria-label` («Записаться на время 17:30–18:30, 7 мест свободно, занято 1 из 8»).
    - Заблокированные, заполненные и прошедшие слоты рендерятся как `<div>` с `aria-disabled="true"` без шеврона/стрелки.
    - Разделение статусов выполнено явными словами: «Мест нет», «Зал закрыт», «Время прошло» (не цветом единым).
  - **Оптимизация полосы дат `DateStrip`**:
    - Пересчет индикатора активной даты вынесен в `updateIndicator` по `ResizeObserver` и `document.fonts.ready` без лишних измерений в каждом кадре.
    - Добавлен центрирующий скролл к активной дате: `scrollIntoView({ behavior: 'smooth', block: 'nearest', inline: 'center' })`.
  - **Сохранение контекста и двусторонний переход (Round-Trip Context)**:
    - При клике на слот в `BookingPage` передаются `fromSlotId`, `date` и `trainer` (в URL params и `location.state`).
    - При возврате из `BookingPage` («К расписанию», «Выбрать другое время», «Записаться снова») контекст `date`, `trainer` и `slot` восстанавливается в URL и state.
    - В `SchedulePage` реализовано устойчивое восстановление скролла и фокуса на исходный слот (`#slot-${backSlotId}`) с отказоустойчивым переключением на заголовок дня (`#schedule-day-heading`), сохраняющее фокус при размонтированиях и повторных рендерах React StrictMode.
  - **Открытые секции состояний**:
    - Состояния ошибки, загрузки и пустого расписания переведены в открытый бесплашечный формат (`schedule-state` без фоновых карточек).
- **Верификация**:
  - Vitest: 50/50 PASS (включая 6 новых тестов в `src/__tests__/m04_schedule_context.test.tsx`).
  - Pytest: 40/40 PASS.
  - Build & Lint: 0 errors, 0 warnings.
  - Chrome CDP browser QA (`verify_m04_schedule.mjs`):
    - 320x740: `slotSize: { width: 288, height: 66 }`, `hasHorizontalScroll: false`, `intervalFontSize: 20px`, `statusFontSize: 13px`.
    - 360x800: `slotSize: { width: 328, height: 66 }`, `hasHorizontalScroll: false`, `intervalFontSize: 20px`, `statusFontSize: 13px`.
    - 390x844: `slotSize: { width: 358, height: 71 }`, `hasHorizontalScroll: false`, `intervalFontSize: 21px`, `statusFontSize: 14px`.
    - 430x932: `slotSize: { width: 398, height: 71 }`, `hasHorizontalScroll: false`, `intervalFontSize: 21px`, `statusFontSize: 14px`.
    - Круговой переход (round-trip): скролл и фокус на `#slot-2026-09-11-1730` (`isFocusedOnSlot: true`, `activeElementTag: 'A'`), фильтр тренера сохранен.
    - Скриншоты: `m04_schedule_320x740.png`, `m04_schedule_360x800.png`, `m04_schedule_390x844.png`, `m04_schedule_430x932.png`, `m04_schedule_with_trainer_390x844.png`, `m04_schedule_restored_focus_390x844.png`.
- **Статус**: PASS. Готовность к переходу на M05.

## Этап M05 — Booking и details: достоверное подтверждение без разрыва
- **Дата**: 11.09.2026
- **Изменения**:
  - **Разделение outcome и presentation в mutation flow**:
    - При успехе API вызова `createBooking` гарантированно и немедленно обновляется кэш TanStack Query (`queryClient.setQueryData(['bookings'], ...)`), и инвалидируются зависимые запросы (`schedule`, `home`, `profile`) независимо от флага монтирования компонента (`isMountedRef`) и 350ms визуального таймера.
    - Устранено ошибочное инвалидирование самого ключа `['bookings']` в `refresh()`, предотвращая race condition и перезапись созданной брони пустым рефетчем в тестах/офлайне.
    - Локальный 350ms таймер (`confirmPhase === 'confirmed'`, кнопка `button--confirmed`, «✓ Запись подтверждена») изолирован внутри компонента под защитой `isMountedRef` и тайм-аут рефа. Если пользователь покидает экран до истечения 350ms, кэш TanStack Query уже содержит бронь, и при повторном заходе сразу отображается подтвержденная тренировка.
  - **Маршрутизация и разделение ID брони и ID слота**:
    - В `/training/:id` и `/booking/:id` четко разделены `bookingById` (`b.id === id`), `activeBookingBySlot` (`b.slotId === id && b.status !== 'cancelled'`) и бронь текущей сессии.
    - Если в истории пользователя есть отмененная бронь с тем же `slotId`, она больше не блокирует бронирование открытого слота — компонент позволяет заново забронировать этот слот.
    - Для несуществующих ID слотов/броней отображается открытое картоless пустое состояние с кнопкой возврата в расписание.
  - **Форма бронирования и семантическая доступность**:
    - Варианты формата («Самостоятельно» / выбор тренера) реализованы как нативные `<input type="radio" name="booking-mode">` с оберткой `<fieldset>` и `<legend class="booking-mode__title">`.
    - Поддержка навигации с клавиатуры (Tab, стрелки, Space).
    - Состояние `disabled` для радиокнопок во время отправки и фиксации подтверждения (`confirmPhase !== 'idle'`).
    - Обработка ошибок загрузки тренеров: при сбое `trainersQuery.isError` отображается понятный inline-блок ошибки с кнопкой «Повторить загрузку тренеров», сохраняя возможность записаться самостоятельно.
  - **Вычисление статусов и карточка брони (Details view)**:
    - Строгий расчет статуса:
      * `cancelled` — «Запись отменена», `aria-label` контейнера строго содержит «отмененная тренировка» и никогда не говорит «подтверждена»;
      * `completed` — «Тренировка завершена» (`now >= endTime`);
      * `ongoing` — «Тренировка идёт сейчас» (`now >= startTime && now < endTime`), тренировка корректно считается идущей, а не завершенной в момент старта;
      * `confirmed` — «Запись подтверждена» (`now < startTime`).
  - **Модальное окно отмены и правило 4 часов**:
    - Бизнес-правило 4 часов сохранено: если до начала тренировки < 4ч, отмена через приложение блокируется с отображением понятного предупреждения.
    - При доступной отмене кнопка «Отменить запись» открывает модальное окно подтверждения (`cancel-confirm-modal`) с доступной структурой диалога (`role="dialog"`, `aria-labelledby`).
    - Обработка ошибок отмены: при ошибке API отмены (400, 500 или отказ сервера) ошибка отображается `inline-error` внутри модального окна; модалка не закрывается самопроизвольно.
    - Защита от дублирующих кликов: обе кнопки модалки дизейблятся во время `cancelMutation.isPending`.
    - При успешной отмене модалка закрывается, а экран плавно переключается в статус «Запись отменена» с кнопкой «Выбрать другое время».
  - **Ритм и верстка Details**:
    - Открытая секция без тяжелых плашек: статусная строка → hero-время (`clamp(2.75rem, 10vw, 3.25rem)` с `font-weight: 700`) → факты (дата, название, имя тренера, длительность через буллит `·`) → заполненность зала (только при наличии данных слота) → тонкий разделитель → блок управления записью.
    - Убраны дублирующие uppercase метки (метка «ВАША ТРЕНИРОВКА» сохранена как `.sr-only` для экранных дикторов и стабильности существующих тестов).
    - Полная адаптивность на 4 брейкпоинтах (320, 360, 390, 430px), отсутствие горизонтального скролла, тач-таргеты `>= 44x44px`.
- **Верификация**:
  - Vitest: 61/61 PASS (включая 11 новых тестов в `src/__tests__/m05_booking_flow.test.tsx`).
  - Pytest: 40/40 PASS.
  - Build & Lint: 0 errors, 0 warnings.
  - Chrome CDP browser QA (`verify_m05_booking.mjs`):
    - 320x740: `hasHorizontalScroll: false`, `radioSize: { width: 288, height: 66 }`, `submitSize: { width: 288, height: 48 }`.
    - 360x800: `hasHorizontalScroll: false`, `radioSize: { width: 328, height: 66 }`, `submitSize: { width: 328, height: 48 }`.
    - 390x844: `hasHorizontalScroll: false`, `radioSize: { width: 358, height: 66 }`, `submitSize: { width: 358, height: 48 }`.
    - 430x932: `hasHorizontalScroll: false`, `radioSize: { width: 398, height: 66 }`, `submitSize: { width: 398, height: 48 }`.
    - 350ms visual hold: зафиксирована кнопка `button--confirmed` («✓ Запись подтверждена») на 120ms.
    - Confirmed Details view: hero time 34.4–52px, statusText «Запись подтверждена», `cancelBtnHeight: 48px`.
    - Cancel modal: `isModalOpen: true`, `title: 'Отменить запись?'`, `keepBtnHeight: 48px`, `cancelBtnHeight: 48px`.
    - Cancelled Details view: `statusText: 'Запись отменена'`, `actionBtnText: 'Выбрать другое время'`.
    - Скриншоты: `m05_booking_form_320x740.png`, `m05_booking_form_360x800.png`, `m05_booking_form_390x844.png`, `m05_booking_form_430x932.png`, `m05_booking_confirmed_hold_390x844.png`, `m05_details_confirmed_390x844.png`, `m05_cancel_modal_390x844.png`, `m05_details_cancelled_390x844.png`.
- **Статус**: PASS. Готовность к переходу на M06.

## Этап M06 — Trainers: открытая editorial entity
- **Дата**: 11.09.2026
- **Изменения**:
  - **Открытый editorial-формат вместо коробочных карточек**:
    - Устранена закрытая блочная карточка (`.trainer-card` с 4-сторонней границей и сплошным фоном); каждая сущность тренера оформлена как открытая редакционная статья (`<article className="trainer-entity trainer-card">`) с прозрачным фоном и тонким 1px разделителем (`.trainer-list__divider`) между элементами.
    - Вертикальный ритм: отступ между сущностями тренеров 28–32px.
  - **Брендовая монограмма и типографика**:
    - Аватар монограммы: 56×56px (`.trainer-avatar`), фон `#191919`, скругление `--radius-control` (4px), акцентная желтая буква имени (`#FFD626`, `font-weight: 850`, `font-size: 1.5rem`), скрыта от скринридеров (`aria-hidden="true"`). Сохранен View Transition ID (`trainer-avatar-${trainer.id}`).
    - Имя тренера: крупный редакционный заголовок `<h2>` (`clamp(1.65rem, 5.2vw, 1.95rem)` / ~28–32px, `font-weight: 800`, `letter-spacing: -0.025em`) с естественным переносом длинных имен (`overflow-wrap: break-word`, `word-break: normal`) без троеточий и искусственного срезания.
  - **Независимые действия-сиблинги (без вложенной интерактивности)**:
    - Разделены зоны перехода: верхний блок идентификации обернут в `<Link to={'/trainers/' + trainer.id}>` (`.trainer-entity__identity-link`) со стрелкой `<ChevronRight size={20} />` и доступным лейблом.
    - Действие записи: независимая сиблинг-кнопка `<ButtonLink to={'/schedule?trainer=' + trainer.id} variant="secondary">Выбрать время</ButtonLink>` (`.trainer-entity__schedule-btn`) с высотой 44px и сенсорной зоной `>= 44x44px`.
    - Исключена любая вложенность интерактивных элементов (`<Link>` внутри `<button>` или `<button>` внутри `<Link>`).
  - **Сохранение авторских направлений и доступный disclosure**:
    - Искоренена деструктивная нормализация `normalizeSpecialty` (которая сжимала «набор мышечной массы / ОФП» до «Набор массы»). Создана бережная функция `cleanSpecialty`, сохраняющая авторский текст и регистр.
    - Список направлений оформлен в виде буллитов `·` (`.trainer-entity__spec-item`).
    - Для тренеров с $> 3$ направлениями реализовано доступное раскрытие: кнопка «Все направления (N)» / «Свернуть» с атрибутами `aria-expanded` и `aria-controls="trainer-specs-${trainer.id}"` с минимальной высотой 44px. При $\le 3$ направлениях кнопка раскрытия не отображается.
  - **Состояния экрана тренеров (`TrainersPage.tsx`)**:
    - Состояние загрузки: семантический скелетон (`.trainer-list--loading`, `aria-busy="true"`).
    - Состояние ошибки: информативное сообщение с красным оттенком (`role="alert"`) и кнопка «Повторить попытку» с иконкой обновления, вызывающая `refetch()`.
    - Пустое состояние: вежливое пояснение о скором обновлении списка (`role="status"`).
    - Секция самостоятельной тренировки: переведена из закрытой плашки в открытую строку футера (`.self-training-row`) с тонким верхним разделителем, заголовком «Хотите заниматься самостоятельно?» и вторичной кнопкой «Выбрать время →», ведущей на `/schedule`. Секция остается доступной во всех состояниях (включая ошибку и пустой список).
- **Верификация**:
  - Vitest: 71/71 PASS (включая 10 новых тестов в `src/__tests__/m06_trainers_editorial.test.tsx`).
  - Pytest: 40/40 PASS.
  - Build & Lint: 0 errors, 0 warnings.
  - Chrome CDP browser QA (`verify_m06_trainers.mjs`, `verify_m06_states_clean.mjs`):
    - 320x740: `hasHorizontalScroll: false`, `avatarSize: 56x56`, `identitySize: 288x64`, `scheduleSize: 153x44`, `disclosureSize: 141x44`, `selfTrainingBtnSize: 288x44`.
    - 360x800: `hasHorizontalScroll: false`, `avatarSize: 56x56`, `identitySize: 328x64`, `scheduleSize: 153x44`, `selfTrainingBtnSize: 169x44`.
    - 390x844: `hasHorizontalScroll: false`, `avatarSize: 56x56`, `identitySize: 358x64`, `scheduleSize: 153x44`, `selfTrainingBtnSize: 169x44`.
    - 430x932: `hasHorizontalScroll: false`, `avatarSize: 56x56`, `identitySize: 398x64`, `scheduleSize: 153x44`, `selfTrainingBtnSize: 169x44`.
    - Раскрытие/сворачивание: переключение `aria-expanded` `false` -> `true` -> `false`, текст «Все направления (4)» <-> «Свернуть».
    - Сиблинг-навигация: клик по «Выбрать время» ведет на `/schedule?trainer=dima`; клик по строке идентификации ведет на `/trainers/dima`.
    - Состояния ошибки и пустого списка: зафиксированы скриншотами на 390x844.
    - Скриншоты: `m06_trainers_320x740.png`, `m06_trainers_360x800.png`, `m06_trainers_390x844.png`, `m06_trainers_430x932.png`, `m06_trainers_expanded_390x844.png`, `m06_trainer_profile_390x844.png`, `m06_trainers_error_390x844.png`, `m06_trainers_empty_390x844.png`.
- **Статус**: PASS. Готовность к переходу на M07.

## Этап M07 — Trainer Profile: единая identity и содержательные секции
- **Дата**: 11.09.2026
- **Изменения**:
  - **Открытый editorial-профиль вместо каскада карточек**:
    - Устранены тяжелые блочные карточки (`.trainer-about`, `.trainer-channel-card`, `.next-slot-card` с закрытыми 4-сторонними рамками и заливками); экран тренера (`TrainerPage.tsx`) оформлен в едином открытом ритме с тонкими разделителями и вертикальным шагом 24–32px.
    - Вьюпорт ограничен shell $\le 430$ px с паддингом `padding-bottom: calc(96px + env(safe-area-inset-bottom, 0px))` для защиты нижнего интерактивного контента от перекрытия навигационной панелью.
  - **Единая Identity и устранение неграмматичного текста**:
    - Монограмма: $64\times 64$ px (`.trainer-avatar`), акцентная желтая буква имени на `#191919`, скругление `--radius-control` (4px), `aria-hidden="true"`, View Transition `trainer-avatar-${trainer.id}`.
    - Имя: заголовок `<h1>` с размером `clamp(2rem, 6.2vw, 2.35rem)` (32–36px), `font-weight: 800`, переносом длинных имен (`overflow-wrap: break-word`, `word-break: normal`) и View Transition `trainer-name-${trainer.id}`.
    - Кнопка действия: неграмматичное «Записаться к Дима» заменено на строгое первичное «Выбрать время» (`variant="primary"`, желтый `#FFD626`, черный текст, min-height 48px, тач-таргет $\ge 48$ px).
  - **Устранение дублирования направлений**:
    - Исключены дублирующие чипсы/теги под именем. Направления представлены единственным содержательным списком в секции «Направления» с буллитами `·` и сохранением авторских формулировок (`cleanSpecialty`).
  - **Честный блок «Ближайшее время в зале»**:
    - Отбор слотов строго по будущему времени (`endIso > now && !isBlocked && occupied < capacity`). Прошедшие слоты исключены.
    - Честная подпись «БЛИЖАЙШЕЕ ВРЕМЯ В ЗАЛЕ» (без ложного обещания персональной доступности тренера).
    - Разделение состояний: доступный слот (кнопка «Выбрать» ведет на `/booking/${slot.id}?trainer=${trainer.id}`), отсутствие свободных слотов («На ближайшие дни свободных мест нет» + кнопка «Все слоты») и сбой расписания («Не удалось загрузить ближайшие слоты» + кнопка «Расписание»). Сбой расписания не ломает отображение профиля тренера.
  - **Разделение ошибки сети и 404 (неизвестного тренера)**:
    - Сетевая ошибка (`trainersQuery.isError`): отображается понятное сообщение (`role="alert"`) с кнопкой «Повторить попытку» (`refetch()`). Жесткий редирект на `/404` устранен.
    - Неизвестный тренер (`!trainer`): отображается статусное сообщение «Тренер не найден» с доступной кнопкой возврата «Вернуться к списку тренеров» (`/trainers`). Жесткий редирект на `/404` устранен.
  - **Соблюдение границ контракта бронирования**:
    - Любой тренер, пришедший из API (например, `id: 'sergey'`), полноценно рендерит свой профиль (монограмма, имя, направления, описание).
    - Запись (`/schedule?trainer=...` и `/booking/...`) разрешается только для тренеров, поддерживаемых контрактом `BookingMode` (`isSupportedBookingTrainer`: `'dima' | 'vanya'`). Для неподдерживаемых отображается честное предупреждение «Запись к этому тренеру через приложение временно недоступна» (действие не превращается молча в `self`).
  - **Telegram-канал**:
    - Отображается только при наличии канала (Дима) как открытый редакционный блок (заголовок «Говер на движениях», авторское описание, внешняя ссылка `https://t.me/goverrun`, `target="_blank"`, `rel="noopener noreferrer"`) без синей иконки-кружка и закрытой плашки.
- **Верификация**:
  - Vitest: 83/83 PASS (включая 12 новых тестов в `src/__tests__/m07_trainer_profile.test.tsx`).
  - Pytest: 40/40 PASS.
  - Build & Lint: 0 errors, 0 warnings.
  - Chrome CDP browser QA (`verify_m07_trainer_profile.mjs`, `verify_m07_scroll.mjs`):
    - 320x740: `hasHorizontalScroll: false`, `avatarSize: 64x64`, `nameFontSize: 32px`, `ctaText: 'Выбрать время'`, `ctaSize: 288x48`, `tgSize: 190x44`, `nextSlotSize: 104x44`.
    - 360x800: `hasHorizontalScroll: false`, `avatarSize: 64x64`, `ctaSize: 328x48`, `tgSize: 193x44`, `nextSlotSize: 104x44`.
    - 390x844: `hasHorizontalScroll: false`, `avatarSize: 64x64`, `ctaSize: 358x48`, `tgSize: 193x44`, `nextSlotSize: 104x44`.
    - 430x932: `hasHorizontalScroll: false`, `avatarSize: 64x64`, `ctaSize: 398x48`, `tgSize: 193x44`, `nextSlotSize: 104x44`.
    - Vanya profile: монограмма «В», отсутствие Telegram-блока, ближайший слот на месте.
    - Краевые состояния: зафиксированы скриншоты «Тренер не найден» (без 404), ошибка сети с кнопкой повтора, неподдерживаемый тренер (с честным предупреждением).
    - Скриншоты: `m07_profile_dima_320x740.png`, `m07_profile_dima_360x800.png`, `m07_profile_dima_390x844.png`, `m07_profile_dima_430x932.png`, `m07_profile_dima_scrolled_390x844.png`, `m07_profile_vanya_390x844.png`, `m07_profile_not_found_390x844.png`, `m07_profile_error_390x844.png`, `m07_profile_unsupported_trainer_390x844.png`.
- **Статус**: PASS. Блок 2 (M04–M07) полностью завершен. Готовность к пакетному ревью Codex R2.

## Этап M08 — Progress: реальные измерения, ясная иерархия, доступный график
- **Дата**: 11.09.2026
- **Изменения**:
  - **Устранение фиктивных данных и фальшивых счетчиков**:
    - Полностью удален верхний блок «Посещения / Недели подряд» (`.progress-summary`, `.summary-stat`), формировавшийся на основе захардкоженных значений в `HealthService.get_progress` (`visits = min(3, history)`, `weeks = 3`).
    - Создан документ `docs/issues/ISSUE-BACKEND-HEALTH-SUMMARY-STREAK.md` с описанием дефекта в бэкенде и рекомендациями по расчету дисциплины на основе реальных данных бронирований.
    - Устранено маскирование ошибок: удален аварийный catch-all fallback в `src/api/progress.ts`, возвращавший статические данные при сбое бэкенда `/api/v1/health/progress`. Реальные ошибки 500 / offline теперь честно отображаются через состояние ошибки с кнопкой повтора (`role="alert"`).
  - **Честный Hero Metric веса и динамика (useCountUp)**:
    - Первое асинхронное значение рендерится мгновенно без анимации из 0 или NaN (`targetValue: number | undefined`), устраняя мелькание.
    - Последующие обновления между известными значениями анимируются плавно ($\le 220$ мс, `--motion-duration-ui`).
    - Исключено кеширование веса в `sessionStorage` как ложного текущего факта.
    - Крупная типографика hero-метрики: число 44–52px (`clamp(2.75rem, 8vw, 3.25rem)`), единица «кг» (16–18px, `font-weight: 600`), дата и источник измерения (12–14px, `font-weight: 500`, `#929292`).
    - Дельта веса отображается с явным указанием базовой даты (например, «−1,2 кг с 1 июня»). Если замер единственный — дельта скрывается.
  - **Автономия вторичных метрик состава тела и корректная терминология**:
    - Процент жира и безжировая масса отображаются автономно, даже если данные о весе полностью отсутствуют (с честной заглушкой «Пока нет данных о весе»).
    - Исключена подмена понятий: показатель `lean_body_mass` строго именуется «Безжировая масса» (в соответствии с физиологическим определением), а не «Мышечная масса».
  - **Спокойный пропорциональный график (ProgressLineChart)**:
    - Координата X точек строго пропорциональна реальным меткам времени `measuredAt` (устранены равномерный шаг по индексам и синтетическая 32-точечная интерполяция).
    - Обработаны все краевые случаи: пустой ряд, 1 точка (центрируется), плоский ряд (одинаковый вес, yMin = yMax), множественные замеры с одинаковым временем (защита от деления на 0).
    - SVG полностью изолирован от скринридеров: `aria-hidden="true"`, устранены интерактивные и фокусируемые элементы внутри SVG.
    - Плавающий SVG tooltip заменен на фиксированный HTML-блок (`.chart-readout`) под графиком с навигационными кнопками Prev / Next ($\ge 44\times 44$ px) и тактильным откликом `haptics.selection()`.
    - Добавлен доступный раскрывающийся список замеров (`<div className="chart-data-disclosure">` с кнопкой `aria-expanded` / `aria-controls`) для клавиатурной навигации и скринридеров.
    - Все сенсорные зоны (кнопки периодов, кнопки навигации считывателя, кнопка раскрытия списка, кнопки шапки «Источники» / «+ Замер») приведены к стандарту $\ge 44\times 44$ px.
- **Верификация**:
  - Vitest: 95/95 PASS (11 файлов тестов, включая 12 тестов в `src/__tests__/m08_progress_measurements.test.tsx`).
  - Pytest: 40/40 PASS.
  - Build & Lint: 0 errors, 0 warnings.
  - Chrome CDP browser QA (`qa/verify_m08_progress.mjs`):
    - Вьюпорты 320x740, 360x800, 390x844, 430x932: `hasHorizontalScroll: false`, `heroVal: '78,2'`, `heroUnit: 'кг'`, `deltaText: '−1,2 кг с 1 июня'`, `readoutVal: '78,2 кг'`, `svgAriaHidden: 'true'`, `interactiveInSvg: 0`, все тач-таргеты $\ge 44$ px.
    - Раскрывающийся список замеров: 6 элементов, клавиатурная доступность.
    - Навигация считывателя Prev / Next: корректный переход к предыдущей точке.
    - Одиночная точка: центрирование `cx: 160`, отсутствие дельты.
    - Отсутствие веса: вторичные метрики («Процент жира», «Безжировая масса») отображаются при пустом весе.
    - Ошибка API 500: отображение `role="alert"` и кнопки повтора.
    - Скриншоты: `m08_progress_320x740.png`, `m08_progress_360x800.png`, `m08_progress_390x844.png`, `m08_progress_430x932.png`, `m08_progress_list_expanded_390x844.png`, `m08_progress_single_point_390x844.png`, `m08_progress_empty_weight_390x844.png`, `m08_progress_error_500_390x844.png`.
- **Статус**: PASS. Готовность к переходу на M09.

## Этап M09 — Profile: identity, абонемент и ясные действия
- **Дата**: 11.09.2026
- **Изменения**:
  - **Открытая Identity-строка**:
    - Переведена из вертикального наследуемого стека в явную строчную компоновку (`flex-direction: row`, `flex-wrap: wrap`, gap 16px) без закрывающего контейнера/плашки.
    - Аватар 52×52px с нейтральным фоном `#191919` и скруглением 4px (`--radius-control`).
    - Имя пользователя: заголовок 24–28px (`clamp(1.5rem, 5vw, 1.75rem)`, `font-weight: 800`, `letter-spacing: -0.025em`) с естественным переносом длинных имен (`word-break: normal`, `overflow-wrap: break-word`) без обрезания и троеточий.
    - Город 14px с иконкой `MapPin` (14px).
  - **Достоверный абонемент (`MembershipCard.tsx`)**:
    - Сохранен семантический примитив `<Section className="membership-card">`.
    - Единая базовая линия и нейтральная геометрия иконки `TicketCheck` (44×44px, `#191919`, без желтого ящика) для активного и пустого состояний.
    - Точная дифференциация состояний: `active` (остаток 36–40px `clamp(2.25rem, 7vw, 2.5rem)`, подпись «из 8 / посещений осталось»), `low 1` (предупреждение словом «Осталось 1 последнее посещение»), `zero/depleted` (остаток `0 из 8`, статус «Все посещения использованы»), `expired` (статус словом «Срок действия истёк»), `none` («Нет активного абонемента»), `unlimited` (название строго из `membership.title`, статус «Безлимит», подпись «Неограниченно посещений», без превращения в «на 0 посещений»).
    - Срок действия: обычные текстовые метаданные (13px, `color: var(--text-secondary)`) без капсулы, границы и цветного фона. Если `expiresAt` отсутствует в API, дата не выдумывается.
    - Продление: понятное объяснение обращения к администратору клуба без вымышленного флоу покупки.
  - **История посещений и доступное модальное окно**:
    - На экране профиля отображаются 3 последних реальных посещения; при 0 записей — понятное пустое состояние с CTA-ссылкой на `/schedule`.
    - Кнопка «Вся история» открывает общее модальное окно `Modal` с заголовком (`h2#history-modal-title`), фокус-трэпом и кнопкой закрытия 44×44px.
    - Внутри модального окна строки отображаются непосредственно без второго закрытого бокса (`.history-list--modal` устранен).
    - Длинные имена тренеров (например, «Константинопольский Константин») переносятся естественным образом (`white-space: normal`, `overflow-wrap: break-word`) без жесткого сжатия и обрезания через `ellipsis`.
  - **Честные настройки и границы действий**:
    - Пункт со сведениями о версии переименован из «Настройки» в «О приложении» («Версия 0.1.0 · Великий Новгород»).
    - Искоренение ложного успеха для пуш-уведомлений: убран фиктивный переключатель в `localStorage`, строка честно помечена как требующая нативного приложения (`is-disabled`, бейдж «Недоступно»).
    - Искоренение фиктивного логаута: убрана модалка с ложным сообщением «В прототипе вы остаётесь в профиле Алексея», зафиксирована честная граница прототипа — disabled-строка «Выход из профиля / Автономный режим без серверной авторизации».
    - Разделение загрузки (скелетон с `aria-busy="true"`) и ошибки сети/офлайна (экран `role="alert"` с кнопкой «Повторить», вызывающей `refetch()`).
    - Ссылка «Источники данных» ведет на канонический экран M02 `/integrations`. Ссылка на «Админ-панель» отображается только для пользователей с ролью `admin`.
- **Верификация**:
  - Vitest: 115/115 PASS (12 файлов тестов, включая 20 тестов в `src/__tests__/m09_profile_account.test.tsx`).
  - Pytest: 40/40 PASS.
  - Build & Lint: 0 errors, 0 warnings.
  - Chrome CDP browser QA (`qa/verify_m09_profile.mjs`):
    - Вьюпорты 320x740, 360x800, 390x844, 430x932: `hasHorizontalScroll: false`, `nameFontSize: 24px`, `avatarSize: 52x52`, `memIconSize: 44x44`, все строки настроек $\ge 44$ px (62–79px), кнопка «Вся история» 44px.
    - Длинное имя на 320x740: `hasHorizontalScroll: false`, естественный перенос на 3 строки без обрезания.
    - Безлимитный абонемент: заголовок сохранен, «Безлимит», дата не выдумана.
    - Истёкший абонемент: статус «Срок действия истёк», пояснение о продлении.
    - Модалка истории: 12 записей, длинное имя тренера полностью читаемо, второй панели нет, кнопка закрытия 44×44px.
    - Ошибка API 500: `hasAlert: true`, кнопка «Повторить».
    - Скриншоты: `m09_profile_320x740.png`, `m09_profile_360x800.png`, `m09_profile_390x844.png`, `m09_profile_430x932.png`, `m09_profile_long_name_320x740.png`, `m09_profile_unlimited_390x844.png`, `m09_profile_expired_390x844.png`, `m09_profile_history_modal_390x844.png`, `m09_profile_error_500_390x844.png`.
- **Статус**: PASS. Готовность к переходу на M10.

## Этап M10 — Admin: компактная рабочая композиция
- **Дата**: 11.09.2026
- **Изменения**:
  - **Семантическая доступность и замена некорректной ARIA-таблицы**:
    - Устранена некорректная ARIA-структура (`role="table"` со строками без ячеек `cell`/`columnheader`) и сжимавшаяся до 56px колонка тренера.
    - Реализован семантический список `<ul className="admin-bookings-list" aria-label="Записи на день">` с элементами `<li className="admin-booking-item">` в grid-раскладке (время 18–20px `tabular-nums`, имя клиента 15–16px с естественным переносом `overflow-wrap: break-word`, тренер 12–14px, словесный статус 12–14px).
    - При 0 записей выводится доступное пустое состояние «На этот день записей нет» (`role="status"`).
    - Заголовок секции выводит грамматически точный счетчик: `{bookings.length} ${pluralize(bookings.length, 'клиент', 'клиента', 'клиентов')}`.
  - **Устранение технического текста и небрежных радиусов**:
    - Устранены фразы «Данные из SQLite» и «Она сохранится в базе.», замененные на понятные операционные подписи («Управление слотами на сегодня», «Запись на {date}, {time}»).
    - Произвольные радиусы (11px, 12px, 14px) приведены к токенам foundation: 0px для контента, 4px (`--radius-control`) для контролов и карточек, 8px (`--radius-modal`) для модального окна.
  - **Таймлайн и управление слотами**:
    - Полные слоты (`occupied >= capacity`) оформлены нейтрально (`.is-full` с вторичным текстом) и больше не создают ложного впечатления выбранного состояния.
    - Обратная связь операций: при вызове мутации блокировки/разблокировки все кнопки слотов блокируются от повторных кликов (`disabled`), а на изменяемом слоте выводится статус `Обновление…`.
  - **Управление вместимостью (Capacity Control)**:
    - Секция оформлена как доступная группа `role="group" aria-label="Вместимость зала"`.
    - Кнопки степпера имеют сенсорную зону $\ge 44\times 44$ px и явные метки `aria-label="Уменьшить вместимость"` и `aria-label="Увеличить вместимость"`.
    - Значение вместимости выводится в живой регион `<output aria-live="polite" aria-atomic="true">`.
    - Границы 1–20: кнопки дизейблятся при достижении крайних значений (`capacity <= 1` и `capacity >= 20`) и во время ожидания ответа сервера. При ошибке выводится сообщение `role="alert"`.
  - **Модальное окно добавления клиента**:
    - Использует общий компонент `Modal` с кнопкой закрытия 44×44px и связкой `titleId="add-client-title"`.
    - Явное связывание: `<label htmlFor="client-name-input">` и `<input id="client-name-input">`.
    - Ошибка отправки: при сбое `addAdminBooking` сообщение об ошибке отображается внутри формы модалки (`role="alert"`), введенное имя клиента не сбрасывается, модалка не закрывается.
    - Защита от дублирования: кнопка отправки и инпут блокируются во время `bookingMutation.isPending`.
  - **Обработка сетевых ошибок и безопасные отступы**:
    - Сбой запросов админ-панели честно отображает семантическое состояние ошибки (`schedule-state-card--error`, `role="alert"`) с кнопкой «Повторить» (`refetch()`).
    - Мобильная шапка получила безопасное позиционирование: кнопка «Назад» на левом краю, подпись «Админ-панель» выровнена направо с отступом `margin-left: auto`.
- **Верификация**:
  - Vitest: 128/128 PASS (13 файлов тестов, включая 13 тестов в `src/__tests__/m10_admin_composition.test.tsx`).
  - Pytest: 40/40 PASS.
  - Build & Lint: 0 errors, 0 warnings.
  - Chrome CDP browser QA (`qa/verify_m10_admin.mjs`):
    - Вьюпорты 320x740, 360x800, 390x844, 430x932: `hasHorizontalScroll: false`, все тач-таргеты $\ge 44$ px (кнопка назад 66–68×44, кнопка записи 288–398×44, слоты таймлайна 91–127×100, кнопки степпера 44×44).
    - Длинные имена на 320x740: `hasHorizontalScroll: false`, естественный перенос строк без горизонтального скролла.
    - Пустое состояние на 390x844: `emptyFound: true`, `role: 'status'`, текст «На этот день записей нет».
    - Модальное окно: тач-таргет закрытия 44×44 px, ошибка мутации внутри формы, введенное имя сохранено.
    - Ошибка API 500: карточка `schedule-state-card--error` с кнопкой повтора 116×48 px.
    - Скриншоты: `m10_admin_320x740.png`, `m10_admin_360x800.png`, `m10_admin_390x844.png`, `m10_admin_430x932.png`, `m10_admin_long_names_320x740.png`, `m10_admin_empty_bookings_390x844.png`, `m10_admin_add_modal_error_390x844.png`, `m10_admin_error_500_390x844.png`.
- **Статус**: PASS. Готовность к переходу на M11.

## Этап M11 — Motion continuity: реальные переходы готовых экранов
- **Дата**: 12.09.2026
- **Изменения**:
  - **Активация Data Router (React Router 7)**:
    - `src/App.tsx` переведен с устаревшего `<Routes>` на декларативный Data Router: `createRoutesFromElements` + `createBrowserRouter` + `<RouterProvider router={router} />`.
    - Добавлен `RootLayout` с `<BookingProvider><Outlet /></BookingProvider>`, обеспечивающий сохранение контекста уведомлений и бронирования при любых переходах.
    - В `src/main.tsx` удален устаревший `<BrowserRouter>`, `QueryClientProvider` сохранен на верхнем уровне.
    - Активирован вызов нативного `document.startViewTransition()` при переходах по компонентам с пропом `viewTransition`.
  - **Три связных перехода (Shared Continuity Pairs)**:
    1. **Home slot time $\to$ Booking hero time (`hero-slot-time`)**:
       - `BookingCard.tsx`: время слота имеет `style={{ viewTransitionName: 'hero-slot-time' }}`, кнопка «Записаться» / «Подробнее» использует `viewTransition`.
       - `BookingPage.tsx`: заголовок времени тренировки (`training-details__time`) связывает `hero-slot-time`.
    2. **Schedule selected slot $\to$ Booking hero time (`hero-slot-time`)**:
       - `SchedulePage.tsx`: динамическое присвоение `viewTransitionName: 'hero-slot-time'` строго кликнутому слоту (`activeSlotId`) или возвращаемому слоту (`backSlotId`), исключая дубликаты имен в DOM.
       - Ссылка назад «К расписанию» передает `fromSlotId` в state и использует `viewTransition` для обратного морфинга.
    3. **Trainers list $\to$ Trainer profile (`trainer-avatar-${id}`, `trainer-name-${id}`)**:
       - `TrainerCard.tsx`: аватар и заголовок карточки тренера несут уникальные имена `trainer-avatar-${id}` и `trainer-name-${id}`, ссылка на профиль использует `viewTransition`.
       - `TrainerPage.tsx`: герой-аватар и заголовок страницы тренера несут соответствующие имена. Кнопка возврата «Все тренеры» имеет `viewTransition`.
  - **Обычная навигация без morphing**:
    - Home «Другое время →» ведет на `/schedule` через стандартный root-кроссфейд страницы без морфинга времени.
    - Кнопка тренера «Выбрать время» ведет на `/schedule?trainer=${id}` без морфинга аватара/имени.
    - Нижняя навигация (`MobileBottomNav`): лаконичный скользящий индикатор на CSS `transform: translateX(...)` с активным состоянием пресса (`var(--motion-press-scale)`), без полноэкранных анимаций.
  - **Процесс бронирования (350мс hold и независимость кэша)**:
    - При успешной мутации `createBooking` TanStack Query кэш `['bookings']` обновляется немедленно до запуска таймеров анимации.
    - Кнопка удерживает подтвержденное состояние `.button--confirmed` ровно 350мс, после чего плавно переходит в `training-details-page` через `safeStartViewTransition`.
  - **Доступность и graceful fallback**:
    - В `src/styles/motion.css` добавлены правила `height: 100%; width: 100%;` для псевдоэлементов аватаров и имен тренеров, предотвращая геометрические искажения при переходе.
    - При активном `prefers-reduced-motion: reduce` все переходы View Transitions отключаются (`animation: none !important`), а `safeStartViewTransition` выполняет прямой синхронный вызов.
    - В средах без поддержки View Transitions API React Router и `safeStartViewTransition` выполняют моментальное обновление состояния без ошибок.
  - **Стабилизация тестов времени суток**:
    - В `src/__tests__/m05_booking_flow.test.tsx` и `src/__tests__/m07_trainer_profile.test.tsx` системное время зафиксировано в `beforeEach` (`vi.useFakeTimers({ toFake: ['Date'] })` и `vi.setSystemTime(new Date('2026-09-12T08:00:00+03:00'))`), устраняя зависимость от реального времени запуска тестов в течение дня.
- **Верификация**:
  - Vitest: 135/135 PASS (14 файлов тестов, включая 7 новых тестов в `src/__tests__/m11_motion_continuity.test.tsx`).
  - Pytest: 40/40 PASS.
  - Build & Lint: 0 errors, 0 warnings.
  - Chrome CDP browser QA (`qa/verify_m11_motion.mjs`):
    - Вьюпорты 320x740, 360x800, 390x844, 430x932: `hasHorizontalScroll: false`.
    - Home $\to$ Booking: `hero-slot-time` зафиксирован на обоих экранах, кнопка перехода 358×52 px.
    - Schedule $\to$ Booking: до клика 0 слотов с `hero-slot-time`, кликнутый слот получает имя, переход на Booking подтвержден, кнопка возврата 122×44 px.
    - Booking flow: фаза отправки `.button--submitting` («Записываем…»), фаза удержания 350мс `.button--confirmed` («✓ Запись подтверждена»), затем переход в экран деталей «Запись подтверждена».
    - Trainers $\to$ Trainer profile: `trainer-avatar-dima` и `trainer-name-dima` зафиксированы на карточке и в герое профиля, CTA 358×48 px, назад 119×44 px.
    - Нижняя навигация: скользящий индикатор точно позиционируется на 5 вкладках (`translateX(0%)` .. `translateX(400%)`).
    - Скриншоты: `m11_motion_home_hero_390x844.png`, `m11_motion_booking_from_home_390x844.png`, `m11_motion_schedule_list_390x844.png`, `m11_motion_booking_from_schedule_390x844.png`, `m11_motion_booking_350ms_hold_390x844.png`, `m11_motion_booking_confirmed_details_390x844.png`, `m11_motion_trainers_list_390x844.png`, `m11_motion_trainer_profile_390x844.png`, `m11_motion_home_320x740.png`, `m11_motion_home_360x800.png`, `m11_motion_home_390x844.png`, `m11_motion_home_430x932.png`.
- **Статус**: PASS. Готовность к переходу на M12.

## Этап M12 — Final consistency QA и закрытие миграции
- **Дата**: 12.09.2026
- **Базовый коммит (Base)**: `58875cb` (M00 baseline)
- **Текущее состояние (HEAD)**: Финальный рабочий срез визуальной миграции M00–M12
- **Изменения и верификация**:
  - **Матрица приёмки (Acceptance Matrix)**:
    - Протестированы все 10 экранов системы на 4 мобильных вьюпортах (320×740, 360×800, 390×844, 430×932) — ровно 40 актуальных views на едином HEAD (`qa/acceptance_m12/acceptance_matrix_m12.json`).
    - Результат: **40/40 PASS**, горизонтальный скролл отсутствует (`hasHorizontalScroll: false`), все сенсорные зоны `>= 44×44` CSS px (`sub44Count: 0`).
  - **Краевые состояния и обработка ошибок (Edge Cases & Errors)**:
    - Сетевые сбои (500 / offline) на экранах Главная, Расписание, Тренеры, Прогресс, Профиль и Админка честно отображают семантический блок `role="alert"` с кнопкой повтора (`refetch()`), без бесконечной загрузки или скрытия ошибки.
    - Профиль несуществующего тренера (`/trainers/unknown`) отображает понятное статусное сообщение с кнопкой возврата в список без жесткого редиректа на `/404`.
    - Пустые состояния (0 тренеров, 0 свободных слотов, 0 записей в админке) отображают вежливые информативные сообщения (`role="status"`).
  - **Длинный русский контент на узком экране (320×740)**:
    - Длинные ФИО («Константинопольский Константин», «Дмитрий Александрович Константинопольский») переносятся естественным образом (`word-break: normal`, `overflow-wrap: break-word`) без обрезания и троеточий.
    - Раскрывающийся список направлений тренеров корректно разворачивается на 320px без сдвига верстки.
  - **Модальная система и формы**:
    - Зафиксированы скриншоты 5 ключевых модальных окон: клубная информация на главной, подтверждение отмены тренировки, история тренировок в профиле, ручной ввод замера в прогрессе, добавление клиента в админке с обработкой серверной ошибки внутри формы.
    - Подтверждена изоляция фонового DOM (`inert`, `aria-hidden`), кнопка закрытия `>= 44×44` px, LIFO закрытие по Escape в веб-интерфейсе и обработчик Capacitor App Back. **Ограничение**: физическое мобильное устройство Android не подключалось (`adb devices` пуст в локальном окружении Windows), тест на реальном физическом железе отмечен как **НЕ ТЕСТИРОВАЛСЯ**; верификация проведена на уровне веб-истории браузера и эмуляции событий `@capacitor/app`.
  - **Масштабирование текста (200% Text Zoom)**:
    - Проверено масштабирование на 390×844 при факторе 2.0: элементы не перекрываются, горизонтальное отсечение отсутствует.
  - **Сквозные пользовательские сценарии (E2E Flows)**:
    1. Home $\to$ Schedule $\to$ Booking create $\to$ Submitting $\to$ Confirmed (350 ms hold) $\to$ Details $\to$ Cancel $\to$ Cancelled: успешно пройден.
    2. Trainers $\to$ Trainer profile $\to$ Schedule with trainer filter $\to$ Back to Trainer: успешно пройден.
    3. Progress $\to$ Add measurement modal $\to$ Manual weight entry $\to$ Updated chart readout: успешно пройден.
    4. Integrations $\to$ Permissions/info $\to$ Disconnect modal $\to$ Escape LIFO: успешно пройден.
    5. Admin $\to$ Capacity stepper $\to$ Add client modal $\to$ Error handling $\to$ Success: успешно пройден.
  - **Стабильность анимаций (Steady State)**:
    - В steady-state зафиксировано ровно **0 активных анимационных циклов** (`runningCount: 0`). График не выполняет покадровых React-обновлений.
  - **Системные токены и дизайн-система**:
    - Gutter строго 16px, акцентный желтый `#FFD626` используется исключительно для главного CTA и выбранного состояния, радиусы строго 0 (контент), 4px (контролы), 8px (модальные окна). Контраст текста `>= 4.5:1` (обычный) / `>= 3:1` (крупный).
  - **Реестр отдельных продуктовых/бэкенд issues**:
    - `docs/issues/ISSUE-BACKEND-HEALTH-SUMMARY-STREAK.md`: фиктивные недели подряд/посещения в HealthService.get_progress (скрыто в UI до реализации честного алгоритма).
    - `docs/issues/ISSUE-BACKEND-TRAINER-SLOT-AVAILABILITY.md`: уточнен статус валидации тренеров: бэкенд содержит строгий guard `trainer_has_overlap` при бронировании, а ограничение заключается исключительно в публичном превью расписания (отсутствует разбивка свободных мест по тренерам на уровне слота).
    - `docs/issues/ISSUE-AUTH-PROTOTYPE-BOUNDARY.md`: автономный режим работы без серверной авторизации (UI честно обозначает границу без фиктивного логаута).
    - `docs/issues/ISSUE-HEALTH-LEAN-BODY-MASS-TERMINOLOGY.md`: физиологически точная безжировая масса (LBM) вместо подмены понятий «мышечной массой».

## Remediation замечаний Codex Review R3 (Блок 3, M08–M12)
- **Дата**: 12.09.2026
- **Закрытые замечания**:
  - **P1 R3-01 / R2-01 (`src/pages/BookingPage.tsx`)**:
    - Устранено мерцание/срыв 350мс фазы подтверждения при refetch слота до 8/8 занятых мест.
    - Состояние подтверждения (`confirmPhase !== 'idle'`) сделано безусловно приоритетным над текущей вместимостью слота, добавлен `slotSnapshotRef` для стабильного сохранения состояния подтверждения («✓ Запись подтверждена»).
  - **P1 R3-02 / R2-03 (`src/pages/BookingPage.tsx`)**:
    - При переходе по прямой ссылке вида `/booking/s0?trainer=dima` с пустым или несовпадающим списком тренеров активный режим честно сбрасывается в `'self'` («Самостоятельно»), предотвращая рассинхронизацию интерфейса и отправку невалидного ID тренера.
  - **P1 R3-03 / R2-02 (`src/pages/SchedulePage.tsx`)**:
    - При возврате по кнопке «Назад» браузера или WebView истории фокус надежно восстанавливается на карточку слота, с которой был совершен переход, через сохранение ID слота в `history.state` и резервный `sessionStorage`.
  - **P1 R3-04 / R2-05 (`src/styles/index.css`)**:
    - Кнопка сброса фильтра расписания приведена к минимальному сенсорному тач-таргету $\ge 44\times 44$ px (`min-height: 44px; min-width: 44px;`).
    - Недоступные слоты (`.time-slot.is-disabled`) избавлены от `opacity: 0.65`, цвета приведены к системным контрастным токенам `--text-secondary` (#C7C7C7, контрастность 9.3:1) и `--text-muted` (#929292, контрастность 4.68:1), обеспечивая соответствие WCAG AA $\ge 4.5:1$.
  - **P1 R3-05 (`src/components/ProgressLineChart.tsx`)**:
    - Устранено искусственное разнесение по оси X для замеров с одинаковыми временными метками (`tRange === 0`); координаты таких точек теперь строго идентичны (`SVG_WIDTH / 2 = 160px`).
  - **P1 R3-06 (`qa/verify_m12_final_acceptance.mjs`)**:
    - Мок-фикстуры для CDP acceptance QA полностью приведены в соответствие со строгими схемами FastAPI бэкенда (`ApiSchedule`, `HealthProgress`, `ApiBooking[]`, `AdminSettings`).
    - Добавлены строгие runtime assertions отображения hero-метрики прогресса (`78,2 кг`, отсутствие скелетона) и списка записей в админ-панели (наличие записей, отсутствие скелетона).
  - **P1 R3-07 (`MIGRATION_LOG.md`)**:
    - Честно зафиксирован статус нативного аппаратного Back: физическое Android-устройство не подключалось (`adb devices` пуст в локальном окружении), физический тест помечен как **НЕ ТЕСТИРОВАЛСЯ**; подтверждена корректность веб-навигации и слушателя Capacitor App.
  - **P2 R3-08 (`src/styles/index.css`)**:
    - Для админ-панели на экранах $\le 360$ px добавлена адаптивная двухстрочная компоновка карточки записи: время и бейдж статуса в верхней строке, имя клиента и тренер на всю ширину в нижней строке. Устранено сжатие имени до 77px и перенос на 7 строк, исключено переполнение плашки «Самостоятельно».
  - **P2 R3-09 (`docs/issues/ISSUE-BACKEND-TRAINER-SLOT-AVAILABILITY.md`, `MOTION_QA.md`)**:
    - В issue уточнено различие между серверным guard-ом `trainer_has_overlap` при бронировании и публичным расписанием без разделения слотов.
    - В `MOTION_QA.md` дата актуализирована на `12.09.2026`, формулировка раскрытия замеров синхронизирована со структурой `<div className="chart-data-disclosure">`.
- **Итоговая верификация**:
  - Vitest: **138/138 PASS** (14 файлов тестов).
  - Pytest: **40/40 PASS** (бэкенд API).
  - Build & Lint: **0 errors, 0 warnings**.
  - Chrome CDP QA (`qa/verify_m12_final_acceptance.mjs`): **40 views в матрице приёмки PASS**, assertions на Прогресс и Админку PASS, steady state animations = 0.
- **Статус**: **PASS**. Все замечания Codex Review R3 закрыты. Проект готов к деплою и отправке в репозиторий.
